{
  "name": "saasmvp",
  "private": true,
  "version": "0.0.0",
  "description": "Tooling de frontend (E2E smoke tests, futuro build de Tailwind #AUD-11). El backend sigue siendo Django/Python -- ver requirements.txt.",
  "scripts": {
    "test:e2e": "playwright test",
    "build:css": "tailwindcss -i ./static/css/input.css -o ./static/css/output.css --minify",
    "watch:css": "tailwindcss -i ./static/css/input.css -o ./static/css/output.css --watch"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "tailwindcss": "^3.4.19"
  }
}
